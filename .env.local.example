# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: Google OAuth is configured in Supabase Dashboard (Authentication > Providers > Google).
# Redirect URL to add in Supabase: http://localhost:3000/auth/callback
